from typing import Tuple
import numpy as np


def build_model() -> Sequential:
    """
    Builds the machine learning model.

    Returns:
    Sequential: The built model.
    """
    pass


def train_model(model: Sequential, train_images: np.ndarray, train_labels: np.ndarray) -> None:
    """
    Trains the model on the training data.

    Args:
    model (Sequential): The model to train.
    train_images (np.ndarray): The training images.
    train_labels (np.ndarray): The training labels.

    Returns:
    None
    """
    pass
